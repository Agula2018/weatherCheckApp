

//API KEY ->  "2zdLEMfQrhyHB0q8fL4Gly3uAOt9Lutx"

    //Installations
    //Returns list of Installations given by location point, ordered by distance to that point, which are
    // not further than 'maxDistanceKM'. Returns at most 'maxResults' items.
    // If no installation is found within 'maxDistanceKM' from the location point, returns empty list.

    //Measurements

    //Returns detailed measurements (current, historical and future) for a map point, which can be
        //interpolated from nearby installations. History and forecast are in 1h timeframes for 24h
        //in past and future, sorted in ascending order.
        @GET("v2/measurements/point")



//        Gson gson = new GsonBuilder()
//                .setLenient()
//                .create();









        //    //Returns list of IndexTypes supported by the platform
        //    @GET("v2/meta/indexes")
        //    Call<AllMeasurements> metaIndexes(@Header("2zdLEMfQrhyHB0q8fL4Gly3uAOt9Lutx") String apiKey,
        //                                      @Query("lat") double latitude,
        //                                      @Query("lng") double longitude);
        //
        //
        //    //Returns list of MeasurementTypes supported by the platform
        //    @GET("v2/meta/measurements")
        //    Call<AllMeasurements> metaMeasurements(@Header("2zdLEMfQrhyHB0q8fL4Gly3uAOt9Lutx") String apiKey,
        //                                           @Query("lat") double latitude,
        //                                           @Query("lng") double longitude);

